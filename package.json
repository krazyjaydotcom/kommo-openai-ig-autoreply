{
  "name": "zernio-openai-ig-autoreply",
  "version": "1.1.0",
  "private": true,
  "description": "Zernio + OpenAI Instagram DM auto-reply tool with natural conversation safeguards.",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/start.js",
    "check": "node --check server.js && node --check scripts/start.js && node --check scripts/upgrade-conversation-engine.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
